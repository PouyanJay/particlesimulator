// Registers jest-dom matchers (toBeInTheDocument, toHaveFocus, ...) on Vitest's expect.
// Runs before every test file; harmless for node-environment unit tests.
import '@testing-library/jest-dom/vitest'
